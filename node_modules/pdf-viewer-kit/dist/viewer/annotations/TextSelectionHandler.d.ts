import { PageViewport } from 'pdfjs-dist';
type AnnotationType = 'highlight' | 'underline' | 'strike' | 'squiggle';
export declare class TextSelectionHandler {
    private static toolbar;
    private static handlers;
    private static activeHandler;
    private static isMouseSelecting;
    private static currentSelectionRect;
    private static popperInstance;
    private static initialized;
    private static containerId;
    private static currentSelectionRange;
    private readonly pageWrapper;
    private readonly textLayerDiv;
    private readonly annotationLayer;
    private readonly textDivs;
    private readonly viewport;
    constructor(containerId: string, pageWrapper: HTMLElement, textLayerDiv: HTMLElement, annotationLayer: HTMLElement, textDivs: HTMLElement[], viewport: PageViewport);
    private static initializeShared;
    private static destroyShared;
    private static buildToolbar;
    private static onPointerDown;
    private static onPointerUp;
    private static onSelectionChange;
    private static onScroll;
    private static processSelection;
    private static showToolbarAt;
    private static hideToolbar;
    applyAnnotation(type: AnnotationType): void;
    private persist;
    destroy(): void;
}
export {};
